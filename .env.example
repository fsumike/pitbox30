# Supabase Configuration (Frontend - Safe to expose)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# EmailJS Configuration
VITE_EMAILJS_PUBLIC_KEY=your_public_key
VITE_EMAILJS_SERVICE_ID=service_xxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxx

# Stripe Configuration (Frontend - Safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxx

# Capawesome CLI Token (DO NOT PREFIX WITH VITE_ - Keep secret!)
CAPAWESOME_TOKEN=your_capawesome_token

# ===================================================================
# IMPORTANT: Never commit your actual .env file to version control!
# ===================================================================
#
# Keys prefixed with VITE_ are embedded in frontend bundle and are PUBLIC
# Only use VITE_ prefix for keys that are safe to expose:
# - Supabase Anon Key (NOT service role key)
# - Stripe Publishable Key (NOT secret key)
# - EmailJS Public Key (NOT private key)
#
# NEVER use VITE_ prefix for:
# - API secret keys
# - Service role keys
# - Private tokens
# - Authentication tokens
# ===================================================================
